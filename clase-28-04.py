# expresiones regulares, normalmente se los suele conocer como regexn
# re.search - busca si esta o no objeto mencionado
# re.findall - lista todas las ocurrencias
# re.split - lista elementos sin el separador
# re.sub - string reemplazado por otro string

import re

texto = "Esto es una frase para usar de modelo en la explicacion de expresiones regulares"

x = []

#print(id(x))

if id(x):
    print("true")

else:

    print("false")
